# @package _global_

defaults:
  - /experiment/sampling/mdlm@_global_
  - override /sampling: starshape

sampling:
  sampler:
    t_on: 0.55
    t_off: 0.05
    remasker_schedule: default

samples_path: ${base_samples_path}/starshape_t_on_${sampling.sampler.t_on}_t_off_${sampling.sampler.t_off}_remasker_schedule_${sampling.sampler.remasker_schedule}_len_${max_length}_steps_${num_steps}.pt
